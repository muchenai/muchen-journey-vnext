# Shared home R2

R2 pins the independently built shared-home candidate to controller commit `4df67ff`. The original manifest's
only mismatch was the hash of the entire shared stylesheet after later, out-of-bound map drafts appeared in the
source worktree. R2 deliberately excludes those drafts and changes no homepage runtime or browser evidence.

The candidate remains `HUMAN_NOT_RUN / RELEASE_NOT_AUTHORIZED`.
